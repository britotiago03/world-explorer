# App identity
spring.application.name=country-service

# Server port
server.port=8085

# PostgreSQL configuration
spring.datasource.url=jdbc:postgresql://db:5432/db
spring.datasource.username=database
spring.datasource.password=database123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA + Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=country_schema